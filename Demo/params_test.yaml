run_name: 'demo_weights' # run index name of the model to be tested (same as for training!)

model_name: 'aicd_demo_weights_finetuned.h5'

dataset_root: 'AICD_strong_shadows_incl_no_change'

test_events_list: 'csv_files/events_strong_shadows_incl_no_change_test.csv'
classes: 'csv_files/classes.csv'

image_size: [128,128,3]

confidence_thresh_seg: 0.5

# for finetuned model: crf_rnn_channel_1_newaxis
# for pretrained model: seg_process_sigmoid
hidden_layer_seg_map_name: 'seg_process_sigmoid'